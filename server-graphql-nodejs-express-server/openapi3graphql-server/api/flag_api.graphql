# 
# Sirqul IoT Platform
# 
# 
# Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
# 
# Version: 3.16
# Contact: info@sirqul.com
# Generated by OpenAPI Generator: https://openapi-generator.tech

# package openapi3graphql-server

# flag_api



input CreateFlagInput {
    # The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
    flagableType: String!, 
    # The flagable object id
    flagableId: Int!, 
    # The unique device identifier that made the request (either deviceId or accountId must be used)
    deviceId: String!, 
    # The unique accountId that made the request (either deviceId or accountId must be used)
    accountId: Int!, 
    # An optional description of why is it being flagged
    flagDescription: String!, 
    # The current location of the user
    latitude: Float!, 
    # The current location of the user
    longitude: Float!
}

input DeleteFlagInput {
    # The unique device identifier that made the request (either deviceId or accountId must be used)
    deviceId: String!, 
    # The unique accountId that made the request (either deviceId or accountId must be used)
    accountId: Int!, 
    # This parameter is deprecated.
    itemBeingFlaggedType: String!, 
    # This parameter is deprecated.
    itemBeingFlaggedId: Int!, 
    # The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
    flagableType: String!, 
    # The flagable object id
    flagableId: Int!
}

input UpdateFlagThresholdInput {
    # The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
    itemBeingFlaggedType: String!, 
    # The threshold value
    threshold: Int!, 
    # The application key
    appKey: String!, 
    # The unique device identifier that made the request (either deviceId or accountId must be used)
    deviceId: String!, 
    # The unique accountId that made the request (either deviceId or accountId must be used)
    accountId: Int!
}

type Mutation {
  # Create Flag
  # Allows a user to flag an object that the user deems inappropriate or offensive. Flagable objects include accounts, albums, album contests, assets, game levels, and theme descriptors
  # @param String! flagableType The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
  # @param Int! flagableId The flagable object id
  # @param String! deviceId The unique device identifier that made the request (either deviceId or accountId must be used)
  # @param Int! accountId The unique accountId that made the request (either deviceId or accountId must be used)
  # @param String! flagDescription An optional description of why is it being flagged
  # @param Float! latitude The current location of the user
  # @param Float! longitude The current location of the user
  # @return [SirqulResponse]
  CreateFlag(input: CreateFlagInput!): SirqulResponse
  # Delete Flag
  # Deletes a flag.
  # @param String! deviceId The unique device identifier that made the request (either deviceId or accountId must be used)
  # @param Int! accountId The unique accountId that made the request (either deviceId or accountId must be used)
  # @param String! itemBeingFlaggedType This parameter is deprecated.
  # @param Int! itemBeingFlaggedId This parameter is deprecated.
  # @param String! flagableType The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
  # @param Int! flagableId The flagable object id
  # @return [SirqulResponse]
  DeleteFlag(input: DeleteFlagInput!): SirqulResponse
  # Update Flag Threshold
  # Update the flag threshold on an object type for a particular application.
  # @param String! itemBeingFlaggedType The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
  # @param Int! threshold The threshold value
  # @param String! appKey The application key
  # @param String! deviceId The unique device identifier that made the request (either deviceId or accountId must be used)
  # @param Int! accountId The unique accountId that made the request (either deviceId or accountId must be used)
  # @return [CountResponse]
  UpdateFlagThreshold(input: UpdateFlagThresholdInput!): CountResponse
}

type Query {
  # Get Flag
  # Gets the details on whether the user has flagged a particular flagable object.
  # @param String! flagableType The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
  # @param Int! flagableId The flagable object id
  # @param String! deviceId The unique device identifier that made the request (either deviceId or accountId must be used)
  # @param Int! accountId The unique accountId that made the request (either deviceId or accountId must be used)
  # @param Float! latitude The current location of the user
  # @param Float! longitude The current location of the user
  # @return [FlagResponse]
  GetFlag(flagableType: String!, flagableId: Int!, deviceId: String!, accountId: Int!, latitude: Float!, longitude: Float!): FlagResponse
  # Get Flag Threshold
  # Get the flag threshold value on an object type for a particular application.
  # @param String! itemBeingFlaggedType The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
  # @param String! appKey The application key
  # @return [CountResponse]
  GetFlagThreshold(itemBeingFlaggedType: String!, appKey: String!): CountResponse
}


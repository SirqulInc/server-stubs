# 
# Sirqul IoT Platform
# 
# 
# Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
# 
# Version: 3.16
# Contact: info@sirqul.com
# Generated by OpenAPI Generator: https://openapi-generator.tech

# package openapi3graphql-server

# invite_api



input AcceptInviteInput {
    # the invite token
    token: String!, 
    # the accountId of the user who is accepting the invite
    accountId: Int!, 
    # the album id associated with this invite (if applicable)
    albumId: Int!, 
    # the mission id associated with this invite (if applicable)
    missionId: Int!, 
    # the album contest id associated with this invite (if applicable)
    albumContestId: Int!, 
    # the offer id associated with this invite (if applicable)
    offerId: Int!, 
    # the offer location id associated with this invite (if applicable)
    offerLocationId: Int!, 
    # the retailer location id associated with this invite (if applicable)
    retailerLocationId: Int!, 
    # the application key
    appKey: String!, 
    # whether to auto-friend the invite sender and receiver
    autoFriend: Boolean!, 
    # whether to mark the event as attending automatically after invite is accepted
    autoAttendEvent: Boolean!, 
    # whether to mark the offer as favorited automatically after invite is accepted
    autoFavoriteOffer: Boolean!, 
    # whether to mark the offer location as favorited automatically after invite is accepted
    autoFavoriteOfferLocation: Boolean!, 
    # whether to mark the retailer location as favorited automatically after invite is accepted
    autoFavoriteRetailerLocation: Boolean!
}

input AlbumContestInviteInput {
    # a unique ID given by the device (deviceId or accountId required)
    deviceId: String!, 
    # the account ID of the user (deviceId or accountId required)
    accountId: Int!, 
    # This parameter is deprecated.
    appId: Int!, 
    # the application key
    appKey: String!, 
    # the album contest to share
    albumContestId: Int!, 
    # the current latitude of the user
    latitude: Float!, 
    # the current longitude of the user
    longitude: Float!
}

input AlbumInviteInput {
    # a unique ID given by the device (deviceId or accountId required)
    deviceId: String!, 
    # the account ID of the user (deviceId or accountId required)
    accountId: Int!, 
    # This parameter is deprecated.
    appId: Int!, 
    # the application key
    appKey: String!, 
    # the album to share
    albumId: Int!, 
    # the current latitude of the user
    latitude: Float!, 
    # the current longitude of the user
    longitude: Float!
}

input EventInviteInput {
    # the account ID of the user making the share
    accountId: Int!, 
    # the application key
    appKey: String!, 
    # The ID of the event listing
    listingId: Int!, 
    # the account ID of a Sirqul user they would like to share an event with
    receiverAccountIds: String!, 
    # The retailer location id of where the event will take place
    retailerLocationId: Int!
}

input GameInviteInput {
    # a unique ID given by the device (deviceId or accountId required)
    deviceId: String!, 
    # the account ID of the user (deviceId or accountId required)
    accountId: Int!, 
    # This parameter is deprecated.
    appId: Int!, 
    # the application key
    appKey: String!, 
    # the game level that the user owns and is giving access to
    gameLevelId: Int!, 
    # the current latitude of the user
    latitude: Float!, 
    # the current longitude of the user
    longitude: Float!
}

input MissionInviteInput {
    # a unique ID given by the device (deviceId or accountId required)
    deviceId: String!, 
    # the account ID of the user (deviceId or accountId required)
    accountId: Int!, 
    # This parameter is deprecated.
    appId: Int!, 
    # the application key
    appKey: String!, 
    # the mission to share
    missionId: Int!, 
    # the current latitude of the user
    latitude: Float!, 
    # the current longitude of the user
    longitude: Float!
}

input OfferInviteInput {
    # the account ID of the user making the share
    accountId: Int!, 
    # the application key
    appKey: String!, 
    # the ID of the offer used to invite to favorite
    offerId: Int!
}

input OfferLocationInviteInput {
    # the account ID of the user making the share
    accountId: Int!, 
    # the application key
    appKey: String!, 
    # the id of the offer location to share
    offerLocationId: Int!
}

input RetailerLocationInviteInput {
    # the account ID of the user making the share
    accountId: Int!, 
    # the application key
    appKey: String!, 
    # The retailer location id of where the event will take place
    retailerLocationId: Int!, 
    # Optional album id to link with the invite
    albumId: Int!
}

type Mutation {
  # Accept Invite
  # Allows a user to accept an invite. The user could also become the inviter&#39;s friend.
  # @param String! token the invite token
  # @param Int! accountId the accountId of the user who is accepting the invite
  # @param Int! albumId the album id associated with this invite (if applicable)
  # @param Int! missionId the mission id associated with this invite (if applicable)
  # @param Int! albumContestId the album contest id associated with this invite (if applicable)
  # @param Int! offerId the offer id associated with this invite (if applicable)
  # @param Int! offerLocationId the offer location id associated with this invite (if applicable)
  # @param Int! retailerLocationId the retailer location id associated with this invite (if applicable)
  # @param String! appKey the application key
  # @param Boolean! autoFriend whether to auto-friend the invite sender and receiver
  # @param Boolean! autoAttendEvent whether to mark the event as attending automatically after invite is accepted
  # @param Boolean! autoFavoriteOffer whether to mark the offer as favorited automatically after invite is accepted
  # @param Boolean! autoFavoriteOfferLocation whether to mark the offer location as favorited automatically after invite is accepted
  # @param Boolean! autoFavoriteRetailerLocation whether to mark the retailer location as favorited automatically after invite is accepted
  # @return [ConsumerInviteResponse]
  AcceptInvite(input: AcceptInviteInput!): ConsumerInviteResponse
  # Invite to Contest
  # Allows a user to invite people to gain access to a contest. This will generate an invite token, which when used, will give the invitee access to a contest (whether it is private or not). The invitee will also become the user&#39;s friend when the invitation is accepted.
  # @param String! deviceId a unique ID given by the device (deviceId or accountId required)
  # @param Int! accountId the account ID of the user (deviceId or accountId required)
  # @param Int! appId This parameter is deprecated.
  # @param String! appKey the application key
  # @param Int! albumContestId the album contest to share
  # @param Float! latitude the current latitude of the user
  # @param Float! longitude the current longitude of the user
  # @return [InviteResponse]
  AlbumContestInvite(input: AlbumContestInviteInput!): InviteResponse
  # Invite to Collection
  # Allows a user to invite people to gain access to a collection. This will generate an invite token, which when used, will give the invitee access to a collection (whether it is private or not). The invitee will also become the user&#39;s friend when the invitation is accepted.
  # @param String! deviceId a unique ID given by the device (deviceId or accountId required)
  # @param Int! accountId the account ID of the user (deviceId or accountId required)
  # @param Int! appId This parameter is deprecated.
  # @param String! appKey the application key
  # @param Int! albumId the album to share
  # @param Float! latitude the current latitude of the user
  # @param Float! longitude the current longitude of the user
  # @return [InviteResponse]
  AlbumInvite(input: AlbumInviteInput!): InviteResponse
  # Invite to Event
  # Allows a user to invite people to attend an event. This will generate an invite token, which when used, will allow the invitee to add the offer to their wallet.
  # @param Int! accountId the account ID of the user making the share
  # @param String! appKey the application key
  # @param Int! listingId The ID of the event listing
  # @param String! receiverAccountIds the account ID of a Sirqul user they would like to share an event with
  # @param Int! retailerLocationId The retailer location id of where the event will take place
  # @return [InviteResponse]
  EventInvite(input: EventInviteInput!): InviteResponse
  # Invite to Game Level
  # Allows a user to invite people to gain access to an album. This will generate an invite token, which when used, will give the invitee access to an album (whether it is private or not). The invitee will also become the user&#39;s friend when the invitation is accepted.
  # @param String! deviceId a unique ID given by the device (deviceId or accountId required)
  # @param Int! accountId the account ID of the user (deviceId or accountId required)
  # @param Int! appId This parameter is deprecated.
  # @param String! appKey the application key
  # @param Int! gameLevelId the game level that the user owns and is giving access to
  # @param Float! latitude the current latitude of the user
  # @param Float! longitude the current longitude of the user
  # @return [InviteResponse]
  GameInvite(input: GameInviteInput!): InviteResponse
  # Invite to Mission
  # Allows a user to invite people to gain access to a mission. This will generate an invite token, which when used, will give the invitee access to a mission (whether it is private or not). The invitee will also become the user&#39;s friend when the invitation is accepted.
  # @param String! deviceId a unique ID given by the device (deviceId or accountId required)
  # @param Int! accountId the account ID of the user (deviceId or accountId required)
  # @param Int! appId This parameter is deprecated.
  # @param String! appKey the application key
  # @param Int! missionId the mission to share
  # @param Float! latitude the current latitude of the user
  # @param Float! longitude the current longitude of the user
  # @return [InviteResponse]
  MissionInvite(input: MissionInviteInput!): InviteResponse
  # Invite to Offer
  # Allows a user to invite people to favorite an offer. This will generate an invite token, which when used, will give the invitee the offer in their favorite&#39;s list.
  # @param Int! accountId the account ID of the user making the share
  # @param String! appKey the application key
  # @param Int! offerId the ID of the offer used to invite to favorite
  # @return [InviteResponse]
  OfferInvite(input: OfferInviteInput!): InviteResponse
  # Invite to Offer Location
  # Allows a user to invite people to favorite an offer location. This will generate an invite token, which when used, will give the invitee the offer location in their favorite&#39;s list.
  # @param Int! accountId the account ID of the user making the share
  # @param String! appKey the application key
  # @param Int! offerLocationId the id of the offer location to share
  # @return [InviteResponse]
  OfferLocationInvite(input: OfferLocationInviteInput!): InviteResponse
  # Invite to Retailer Location
  # Allows a user to invite people to favorite a retailer location. This will generate an invite token, which when used, will give the invitee the retailer location in their favorite&#39;s list.
  # @param Int! accountId the account ID of the user making the share
  # @param String! appKey the application key
  # @param Int! retailerLocationId The retailer location id of where the event will take place
  # @param Int! albumId Optional album id to link with the invite
  # @return [InviteResponse]
  RetailerLocationInvite(input: RetailerLocationInviteInput!): InviteResponse
}

type Query {
  # Get Invite
  # This is used to determine whether an invite token is valid. If the token is valid, this will also return information about who invited the user, and what they are invited to.
  # @param Int! accountId Account ID of the user if they are logged in
  # @param String! token the invite token
  # @param Int! albumId album id to match the invite against (if applicable)
  # @param Int! missionId mission id to match the invite against (if applicable)
  # @param Int! albumContestId album contest id to match the invite against (if applicable)
  # @param Int! offerId offer id to match the invite against (if applicable)
  # @param Int! offerLocationId offer location id to match the invite against (if applicable)
  # @param Int! retailerLocationId retailer location id to match the invite against (if applicable)
  # @param String! appKey the application key
  # @return [SirqulResponse]
  GetInvite(accountId: Int!, token: String!, albumId: Int!, missionId: Int!, albumContestId: Int!, offerId: Int!, offerLocationId: Int!, retailerLocationId: Int!, appKey: String!): SirqulResponse
}


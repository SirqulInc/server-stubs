# 
# Sirqul IoT Platform
# 
# 
# Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
# 
# Version: 3.16
# Contact: info@sirqul.com
# Generated by OpenAPI Generator: https://openapi-generator.tech

# package openapi3graphql-server

# app_data_api



input PostAppDataInput {
    # the game to retrieve the data for, use your application key.
    gameType: String!, 
    # start the search results at a record.
    start: Int!, 
    # limit the search results to some number.
    limit: Int!, 
    # The analytic data AnalyticListResponse
    data: String!, 
    # the device id (deviceId or accountId required).
    deviceId: String!, 
    # the account id of the user (deviceId or accountId required).
    accountId: Int!, 
    # if true then include the game data blob, otherwise don&#39;t include.
    includeGameData: Boolean!, 
    # This parameter is deprecated.
    q: String!, 
    # query string used to filter the search results when looking for community and saved levels.
    keyword: String!, 
    # how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
    sortField: String!, 
    # order the search results descending or ascending when looking for community and saved levels.
    descending: Boolean!, 
    # This parameter is deprecated.
    i: Int!, 
    # This parameter is deprecated.
    l: Int!, 
    # if true then include the game object count, otherwise don&#39;t include.
    gameObjectCount: Boolean!, 
    # restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
    filter: String!, 
    # restrict the search to items created less then date
    dateCreated: Int!, 
    # get levels owned by a particular account
    ownerId: Int!, 
    # get missions by specified id, comman seperated list of long ids
    missionIds: String!, 
    # get games by specified id, comman seperated list of long ids
    gameIds: String!, 
    # get packs by specified id, comman seperated list of long ids
    packIds: String!, 
    # get game levels by specified id, comman seperated list of long ids
    gameLevelIds: String!, 
    # the application version, used to versin the game level data
    appVersion: String!, 
    # default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
    includeHigherVersionPacks: Boolean!, 
    # default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
    includeHigherVersionLevels: Boolean!, 
    # use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
    responseGroups: String!, 
    # the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
    purchaseType: String!
}

input RegenAppDataInput {
    # the account id of the user
    accountId: Int!, 
    # process a specific application, if null process all apps with caches
    appKey: String!, 
    # create a specific version, if null use current version. Be careful if processing all
    buildVersion: String!, 
    # create a specific version, if null use current version. Be careful if processing all
    apiVersion: String!
}

type Mutation {
  # Create App Data
  # Publish the application data structure.  Can be used to save levels   and scores.  It then returns the application data structure.  The basic   structure is a node tree, with the root node being a AppResponse.  The response   contains the user&#39;s profile, messages from the system, and a list of MissionResponse.    A mission can have any number of GameResponses but typically is a single   game type.  A game then has any number of PackResponses which help group   the game levels. Packs are then composed of any number of GameLevelResponses.      Using the various parameters can return the applications default mission   (built-in packs to play), the list of community levels published, the user&#39;s   saved levels, or explicity levels desired.  You can choose to include the   profile or not, or just return parts of the profile.  You can also filter   out game levels that have been published with a higher version of the application
  # @param String! gameType the game to retrieve the data for, use your application key.
  # @param Int! start start the search results at a record.
  # @param Int! limit limit the search results to some number.
  # @param String! data The analytic data AnalyticListResponse
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param Boolean! includeGameData if true then include the game data blob, otherwise don&#39;t include.
  # @param String! q This parameter is deprecated.
  # @param String! keyword query string used to filter the search results when looking for community and saved levels.
  # @param String! sortField how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
  # @param Boolean! descending order the search results descending or ascending when looking for community and saved levels.
  # @param Int! i This parameter is deprecated.
  # @param Int! l This parameter is deprecated.
  # @param Boolean! gameObjectCount if true then include the game object count, otherwise don&#39;t include.
  # @param String! filter restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
  # @param Int! dateCreated restrict the search to items created less then date
  # @param Int! ownerId get levels owned by a particular account
  # @param String! missionIds get missions by specified id, comman seperated list of long ids
  # @param String! gameIds get games by specified id, comman seperated list of long ids
  # @param String! packIds get packs by specified id, comman seperated list of long ids
  # @param String! gameLevelIds get game levels by specified id, comman seperated list of long ids
  # @param String! appVersion the application version, used to versin the game level data
  # @param Boolean! includeHigherVersionPacks default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
  # @param Boolean! includeHigherVersionLevels default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
  # @param String! responseGroups use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
  # @param String! purchaseType the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
  # @return [AppResponse]
  PostAppData(input: PostAppDataInput!): AppResponse
  # Regenerate App Data
  # Regenerate the app data cache for apps
  # @param Int! accountId the account id of the user
  # @param String! appKey process a specific application, if null process all apps with caches
  # @param String! buildVersion create a specific version, if null use current version. Be careful if processing all
  # @param String! apiVersion create a specific version, if null use current version. Be careful if processing all
  # @return [SirqulResponse]
  RegenAppData(input: RegenAppDataInput!): SirqulResponse
}

type Query {
  # Get App Data
  # Get the application data structure.  The basic structure is a   node tree, with the root node being a AppResponse.  The response contains   the user&#39;s profile, messages from the system, and a list of MissionResponse.    A mission can have any number of GameResponses but typically is a single   game type.  A game then has any number of PackResponses which help group   the game levels. Packs are then composed of any number of GameLevelResponses.     Using the various parameters can return the applications default mission   (built-in packs to play), the list of community levels published, the user&#39;s   saved levels, or explicity levels desired.  You can choose to include the   profile or not, or just return parts of the profile.  You can also filter   out game levels that have been published with a higher version of the application.
  # @param Int! start start the search results at a record.
  # @param Int! limit limit the search results to some number.
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param String! gameType the game to retrieve the data for, use your application key.
  # @param Boolean! includeGameData if true then include the game data blob, otherwise don&#39;t include.
  # @param String! q This parameter is deprecated.
  # @param String! keyword query string used to filter the search results when looking for community and saved levels.
  # @param String! sortField how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
  # @param Boolean! descending order the search results descending or ascending when looking for community and saved levels.
  # @param Int! i This parameter is deprecated.
  # @param Int! l This parameter is deprecated.
  # @param Boolean! gameObjectCount if true then include the game object count, otherwise don&#39;t include.
  # @param String! filter restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
  # @param Int! dateCreated restrict the search to items created less then date
  # @param Int! ownerId get levels owned by a particular account
  # @param String! missionIds get missions by specified id, comman seperated list of long ids
  # @param String! gameIds get games by specified id, comman seperated list of long ids
  # @param String! packIds get packs by specified id, comman seperated list of long ids
  # @param String! gameLevelIds get game levels by specified id, comman seperated list of long ids
  # @param String! appVersion the application version, used to versin the game level data
  # @param Boolean! includeHigherVersionPacks default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
  # @param Boolean! includeHigherVersionLevels default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
  # @param String! responseGroups use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
  # @param String! purchaseType the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
  # @return [AppResponse]
  GetAppData(start: Int!, limit: Int!, deviceId: String!, accountId: Int!, gameType: String!, includeGameData: Boolean!, q: String!, keyword: String!, sortField: String!, descending: Boolean!, i: Int!, l: Int!, gameObjectCount: Boolean!, filter: String!, dateCreated: Int!, ownerId: Int!, missionIds: String!, gameIds: String!, packIds: String!, gameLevelIds: String!, appVersion: String!, includeHigherVersionPacks: Boolean!, includeHigherVersionLevels: Boolean!, responseGroups: String!, purchaseType: String!): AppResponse
}


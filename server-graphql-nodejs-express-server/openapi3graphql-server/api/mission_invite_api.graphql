# 
# Sirqul IoT Platform
# 
# 
# Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
# 
# Version: 3.16
# Contact: info@sirqul.com
# Generated by OpenAPI Generator: https://openapi-generator.tech

# package openapi3graphql-server

# mission_invite_api



input CreateMissionInviteInput {
    
    version: Float!, 
    # the device id (deviceId or accountId required).
    deviceId: String!, 
    # the account id of the user (deviceId or accountId required).
    accountId: Int!, 
    # The mission to find the invite for.
    missionId: Int!, 
    # code to be entered for user to join the mission
    joinCode: String!, 
    # Include the game level data with the mission.
    includeGameData: Boolean!
}

input DeleteMissionInviteInput {
    
    version: Float!, 
    # the device id (deviceId or accountId required).
    deviceId: String!, 
    # the account id of the user (deviceId or accountId required).
    accountId: Int!, 
    # The mission to find the invite for (missionId or missionInviteId requried).
    missionId: Int!, 
    # The mission invite id. This checks on the user&#39;s billable for permission (missionId or missionInviteId requried).
    missionInviteId: Int!, 
    # Include the game level data with the mission.
    includeGameData: Boolean!
}

input UpdateMissionInviteInput {
    
    version: Float!, 
    # the device id (deviceId or accountId required).
    deviceId: String!, 
    # the account id of the user (deviceId or accountId required).
    accountId: Int!, 
    # the application key
    appKey: String!, 
    # The mission to find the invite for (missionId or missionInviteId requried).
    missionId: Int!, 
    # The mission invite id. This checks on the user&#39;s billable for permission (missionId or missionInviteId requried).
    missionInviteId: Int!, 
    # the pack id to find the invite for
    packId: Int!, 
    # the game level id to find the invite for
    gameLevelId: Int!, 
    # The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
    status: String!, 
    # This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM}
    permissionableType: String!, 
    # The id of the content being submitted.
    permissionableId: Int!, 
    # Include the game level data with the mission.
    includeGameData: Boolean!
}

type Mutation {
  # Create Mission Invite
  # Create the mission invite. An account can only be invited to a mission one time. For missions that require user submission and reviewing the permissionableType and permissionableId need to be provided.
  # @param Float! version 
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param Int! missionId The mission to find the invite for.
  # @param String! joinCode code to be entered for user to join the mission
  # @param Boolean! includeGameData Include the game level data with the mission.
  # @return [MissionResponse]
  CreateMissionInvite(input: CreateMissionInviteInput!): MissionResponse
  # Delete Mission Invite
  # Update the mission invite status to quit.
  # @param Float! version 
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param Int! missionId The mission to find the invite for (missionId or missionInviteId requried).
  # @param Int! missionInviteId The mission invite id. This checks on the user&#39;s billable for permission (missionId or missionInviteId requried).
  # @param Boolean! includeGameData Include the game level data with the mission.
  # @return [SirqulResponse]
  DeleteMissionInvite(input: DeleteMissionInviteInput!): SirqulResponse
  # Update Mission Invite
  # Update the mission invite status. An account can only be invited to a mission one time. For missions that require user submission and reviewing the permissionableType and permissionableId need to be provided.
  # @param Float! version 
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param String! appKey the application key
  # @param Int! missionId The mission to find the invite for (missionId or missionInviteId requried).
  # @param Int! missionInviteId The mission invite id. This checks on the user&#39;s billable for permission (missionId or missionInviteId requried).
  # @param Int! packId the pack id to find the invite for
  # @param Int! gameLevelId the game level id to find the invite for
  # @param String! status The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
  # @param String! permissionableType This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM}
  # @param Int! permissionableId The id of the content being submitted.
  # @param Boolean! includeGameData Include the game level data with the mission.
  # @return [MissionResponse]
  UpdateMissionInvite(input: UpdateMissionInviteInput!): MissionResponse
}

type Query {
  # Get Mission Invite
  # Get the mission invite. An account can only be invited to a mission one time.
  # @param Float! version 
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param Int! missionId The mission to find the invite for (missionId or missionInviteId requried).
  # @param Int! missionInviteId The mission invite id. This checks on the user&#39;s billable for permission (missionId or missionInviteId requried).
  # @param Boolean! includeGameData Include the game level data with the mission.
  # @param String! includeScores include the scores with the mission
  # @return [MissionResponse]
  GetMissionInvite(version: Float!, deviceId: String!, accountId: Int!, missionId: Int!, missionInviteId: Int!, includeGameData: Boolean!, includeScores: String!): MissionResponse
  # Search Mission Invites
  # Get a list of mission invites that the account has.
  # @param Float! version 
  # @param String! deviceId the device id (deviceId or accountId required).
  # @param Int! accountId the account id of the user (deviceId or accountId required).
  # @param String! appKey the app to retrieve the data for, use your application key.
  # @param String! appVersion the application version, used to version the game level data
  # @param Int! missionId the mission id to filter on
  # @param String! status The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
  # @param Int! lastUpdated Only return invites that have been updated since this date/time (long)
  # @param Int! start The index into the record set to start with.
  # @param Int! limit The total number of record to return.
  # @param String! keyword the keyword to search on
  # @param String! missionTypes 
  # @param Boolean! filterByBillable filter results by the account&#39;s billable
  # @param Boolean! includeGameData Include the game level data with the mission.
  # @return [MissionResponse]
  SearchMissionInvites(version: Float!, deviceId: String!, accountId: Int!, appKey: String!, appVersion: String!, missionId: Int!, status: String!, lastUpdated: Int!, start: Int!, limit: Int!, keyword: String!, missionTypes: String!, filterByBillable: Boolean!, includeGameData: Boolean!): MissionResponse
}


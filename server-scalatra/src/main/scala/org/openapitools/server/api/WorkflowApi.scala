/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.SirqulResponse

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class WorkflowApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "WorkflowApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val runWorkflowOperation = (apiOperation[SirqulResponse]("runWorkflow")
    summary "Run Workflow"
    parameters(pathParam[Double]("version").description(""), queryParam[Long]("accountId").description(""), queryParam[Long]("workflowId").description(""), queryParam[Long]("skuId").description("").optional, queryParam[Int]("versionCode").description("").optional, queryParam[String]("parameters").description("").optional)
  )

  post("/api/:version/workflow/run", operation(runWorkflowOperation)) {
    val version = params.getOrElse("version", halt(400))
    //println("version: " + version)
            val accountId = params.getAs[Long]("accountId")

    //println("accountId: " + accountId)
            val workflowId = params.getAs[Long]("workflowId")

    //println("workflowId: " + workflowId)
            val skuId = params.getAs[Long]("skuId")

    //println("skuId: " + skuId)
            val versionCode = params.getAs[Int]("versionCode")

    //println("versionCode: " + versionCode)
            val parameters = params.getAs[String]("parameters")

    //println("parameters: " + parameters)
  }

}

/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.PathingResponse

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class PathingApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "PathingApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val computePathOperation = (apiOperation[PathingResponse]("computePath")
    summary "Calculate Path"
    parameters(queryParam[String]("data").description(""), queryParam[String]("units").description(""), queryParam[Boolean]("reducePath").description(""), queryParam[Boolean]("directions").description(""))
  )

  get("/pathing/compute", operation(computePathOperation)) {
            val data = params.getAs[String]("data")

    //println("data: " + data)
            val units = params.getAs[String]("units")

    //println("units: " + units)
            val reducePath = params.getAs[Boolean]("reducePath")

    //println("reducePath: " + reducePath)
            val directions = params.getAs[Boolean]("directions")

    //println("directions: " + directions)
  }

}

/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "MissionInviteApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string MissionInviteApi::base = "";

MissionInviteApi::MissionInviteApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void MissionInviteApi::init() {
    setupRoutes();
}

void MissionInviteApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Post(*router, base + "/api/:version/mission/invite/create", Routes::bind(&MissionInviteApi::create_mission_invite_handler, this));
    Routes::Post(*router, base + "/api/:version/mission/invite/delete", Routes::bind(&MissionInviteApi::delete_mission_invite_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/invite/get", Routes::bind(&MissionInviteApi::get_mission_invite_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/invite/search", Routes::bind(&MissionInviteApi::search_mission_invites_handler, this));
    Routes::Post(*router, base + "/api/:version/mission/invite/update", Routes::bind(&MissionInviteApi::update_mission_invite_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&MissionInviteApi::mission_invite_api_default_handler, this));
}

void MissionInviteApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> MissionInviteApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void MissionInviteApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> MissionInviteApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void MissionInviteApi::create_mission_invite_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto joinCodeQuery = request.query().get("joinCode");
        std::optional<std::string> joinCode;
        if (joinCodeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(joinCodeQuery.value(), valueQuery_instance)) {
                joinCode = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
    


        try {





            this->create_mission_invite(version, deviceId, accountId, missionId, joinCode, includeGameData, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionInviteApi::delete_mission_invite_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto missionInviteIdQuery = request.query().get("missionInviteId");
        std::optional<int64_t> missionInviteId;
        if (missionInviteIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionInviteIdQuery.value(), valueQuery_instance)) {
                missionInviteId = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
    


        try {





            this->delete_mission_invite(version, deviceId, accountId, missionId, missionInviteId, includeGameData, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionInviteApi::get_mission_invite_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto missionInviteIdQuery = request.query().get("missionInviteId");
        std::optional<int64_t> missionInviteId;
        if (missionInviteIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionInviteIdQuery.value(), valueQuery_instance)) {
                missionInviteId = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
        auto includeScoresQuery = request.query().get("includeScores");
        std::optional<std::string> includeScores;
        if (includeScoresQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(includeScoresQuery.value(), valueQuery_instance)) {
                includeScores = valueQuery_instance;
            }
        }
    


        try {





            this->get_mission_invite(version, deviceId, accountId, missionId, missionInviteId, includeGameData, includeScores, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionInviteApi::search_mission_invites_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto appVersionQuery = request.query().get("appVersion");
        std::optional<std::string> appVersion;
        if (appVersionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appVersionQuery.value(), valueQuery_instance)) {
                appVersion = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto statusQuery = request.query().get("status");
        std::optional<std::string> status;
        if (statusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(statusQuery.value(), valueQuery_instance)) {
                status = valueQuery_instance;
            }
        }
        auto lastUpdatedQuery = request.query().get("lastUpdated");
        std::optional<int64_t> lastUpdated;
        if (lastUpdatedQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(lastUpdatedQuery.value(), valueQuery_instance)) {
                lastUpdated = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto keywordQuery = request.query().get("keyword");
        std::optional<std::string> keyword;
        if (keywordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(keywordQuery.value(), valueQuery_instance)) {
                keyword = valueQuery_instance;
            }
        }
        auto missionTypesQuery = request.query().get("missionTypes");
        std::optional<std::string> missionTypes;
        if (missionTypesQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(missionTypesQuery.value(), valueQuery_instance)) {
                missionTypes = valueQuery_instance;
            }
        }
        auto filterByBillableQuery = request.query().get("filterByBillable");
        std::optional<bool> filterByBillable;
        if (filterByBillableQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(filterByBillableQuery.value(), valueQuery_instance)) {
                filterByBillable = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
    


        try {





            this->search_mission_invites(version, deviceId, accountId, appKey, appVersion, missionId, status, lastUpdated, start, limit, keyword, missionTypes, filterByBillable, includeGameData, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionInviteApi::update_mission_invite_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto missionInviteIdQuery = request.query().get("missionInviteId");
        std::optional<int64_t> missionInviteId;
        if (missionInviteIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionInviteIdQuery.value(), valueQuery_instance)) {
                missionInviteId = valueQuery_instance;
            }
        }
        auto packIdQuery = request.query().get("packId");
        std::optional<int64_t> packId;
        if (packIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(packIdQuery.value(), valueQuery_instance)) {
                packId = valueQuery_instance;
            }
        }
        auto gameLevelIdQuery = request.query().get("gameLevelId");
        std::optional<int64_t> gameLevelId;
        if (gameLevelIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(gameLevelIdQuery.value(), valueQuery_instance)) {
                gameLevelId = valueQuery_instance;
            }
        }
        auto statusQuery = request.query().get("status");
        std::optional<std::string> status;
        if (statusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(statusQuery.value(), valueQuery_instance)) {
                status = valueQuery_instance;
            }
        }
        auto permissionableTypeQuery = request.query().get("permissionableType");
        std::optional<std::string> permissionableType;
        if (permissionableTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(permissionableTypeQuery.value(), valueQuery_instance)) {
                permissionableType = valueQuery_instance;
            }
        }
        auto permissionableIdQuery = request.query().get("permissionableId");
        std::optional<int64_t> permissionableId;
        if (permissionableIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(permissionableIdQuery.value(), valueQuery_instance)) {
                permissionableId = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
    


        try {





            this->update_mission_invite(version, deviceId, accountId, appKey, missionId, missionInviteId, packId, gameLevelId, status, permissionableType, permissionableId, includeGameData, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void MissionInviteApi::mission_invite_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api


/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "AssetApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string AssetApi::base = "";

AssetApi::AssetApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void AssetApi::init() {
    setupRoutes();
}

void AssetApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Get(*router, base + "/api/:version/asset/download/:filename", Routes::bind(&AssetApi::asset_download_handler, this));
    Routes::Post(*router, base + "/api/:version/asset/morph", Routes::bind(&AssetApi::asset_morph_handler, this));
    Routes::Post(*router, base + "/api/:version/asset/create", Routes::bind(&AssetApi::create_asset_handler, this));
    Routes::Post(*router, base + "/api/:version/asset/delete", Routes::bind(&AssetApi::delete_asset_handler, this));
    Routes::Get(*router, base + "/api/:version/asset/get", Routes::bind(&AssetApi::get_asset_handler, this));
    Routes::Post(*router, base + "/api/:version/asset/remove", Routes::bind(&AssetApi::remove_asset_handler, this));
    Routes::Get(*router, base + "/api/:version/asset/search", Routes::bind(&AssetApi::search_assets_handler, this));
    Routes::Post(*router, base + "/api/:version/asset/update", Routes::bind(&AssetApi::update_asset_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&AssetApi::asset_api_default_handler, this));
}

void AssetApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> AssetApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void AssetApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> AssetApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void AssetApi::asset_download_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        auto filename = request.param(":filename").as<std::string>();
        
        
    


        try {





            this->asset_download(version, filename, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::asset_morph_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto offerIdQuery = request.query().get("offerId");
        std::optional<int64_t> offerId;
        if (offerIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(offerIdQuery.value(), valueQuery_instance)) {
                offerId = valueQuery_instance;
            }
        }
        auto creativeIdQuery = request.query().get("creativeId");
        std::optional<int64_t> creativeId;
        if (creativeIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(creativeIdQuery.value(), valueQuery_instance)) {
                creativeId = valueQuery_instance;
            }
        }
        auto adSizeQuery = request.query().get("adSize");
        std::optional<std::string> adSize;
        if (adSizeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(adSizeQuery.value(), valueQuery_instance)) {
                adSize = valueQuery_instance;
            }
        }
        auto widthQuery = request.query().get("width");
        std::optional<int32_t> width;
        if (widthQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(widthQuery.value(), valueQuery_instance)) {
                width = valueQuery_instance;
            }
        }
        auto heightQuery = request.query().get("height");
        std::optional<int32_t> height;
        if (heightQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(heightQuery.value(), valueQuery_instance)) {
                height = valueQuery_instance;
            }
        }
        auto backgroundSizeQuery = request.query().get("backgroundSize");
        std::optional<std::string> backgroundSize;
        if (backgroundSizeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(backgroundSizeQuery.value(), valueQuery_instance)) {
                backgroundSize = valueQuery_instance;
            }
        }
        auto r_templateQuery = request.query().get("template");
        std::optional<std::string> r_template;
        if (r_templateQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(r_templateQuery.value(), valueQuery_instance)) {
                r_template = valueQuery_instance;
            }
        }
    


        try {





            this->asset_morph(version, offerId, adSize, creativeId, width, height, backgroundSize, r_template, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::create_asset_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {
            
            try {
                this->create_asset(request, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::delete_asset_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto assetIdQuery = request.query().get("assetId");
        std::optional<std::string> assetId;
        if (assetIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(assetIdQuery.value(), valueQuery_instance)) {
                assetId = valueQuery_instance;
            }
        }
        auto latitudeQuery = request.query().get("latitude");
        std::optional<double> latitude;
        if (latitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(latitudeQuery.value(), valueQuery_instance)) {
                latitude = valueQuery_instance;
            }
        }
        auto longitudeQuery = request.query().get("longitude");
        std::optional<double> longitude;
        if (longitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(longitudeQuery.value(), valueQuery_instance)) {
                longitude = valueQuery_instance;
            }
        }
    


        try {





            this->delete_asset(version, assetId, deviceId, accountId, latitude, longitude, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::get_asset_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto assetIdQuery = request.query().get("assetId");
        std::optional<int64_t> assetId;
        if (assetIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(assetIdQuery.value(), valueQuery_instance)) {
                assetId = valueQuery_instance;
            }
        }
        auto noteDescendingQuery = request.query().get("noteDescending");
        std::optional<bool> noteDescending;
        if (noteDescendingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(noteDescendingQuery.value(), valueQuery_instance)) {
                noteDescending = valueQuery_instance;
            }
        }
    


        try {





            this->get_asset(version, assetId, deviceId, accountId, noteDescending, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::remove_asset_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto albumIdQuery = request.query().get("albumId");
        std::optional<int64_t> albumId;
        if (albumIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(albumIdQuery.value(), valueQuery_instance)) {
                albumId = valueQuery_instance;
            }
        }
        auto collectionIdQuery = request.query().get("collectionId");
        std::optional<int64_t> collectionId;
        if (collectionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(collectionIdQuery.value(), valueQuery_instance)) {
                collectionId = valueQuery_instance;
            }
        }
        auto assetIdQuery = request.query().get("assetId");
        std::optional<std::string> assetId;
        if (assetIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(assetIdQuery.value(), valueQuery_instance)) {
                assetId = valueQuery_instance;
            }
        }
        auto removeFromDefaultAlbumsQuery = request.query().get("removeFromDefaultAlbums");
        std::optional<bool> removeFromDefaultAlbums;
        if (removeFromDefaultAlbumsQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(removeFromDefaultAlbumsQuery.value(), valueQuery_instance)) {
                removeFromDefaultAlbums = valueQuery_instance;
            }
        }
        auto latitudeQuery = request.query().get("latitude");
        std::optional<double> latitude;
        if (latitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(latitudeQuery.value(), valueQuery_instance)) {
                latitude = valueQuery_instance;
            }
        }
        auto longitudeQuery = request.query().get("longitude");
        std::optional<double> longitude;
        if (longitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(longitudeQuery.value(), valueQuery_instance)) {
                longitude = valueQuery_instance;
            }
        }
    


        try {





            this->remove_asset(version, assetId, deviceId, accountId, albumId, collectionId, removeFromDefaultAlbums, latitude, longitude, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::search_assets_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto albumIdsQuery = request.query().get("albumIds");
        std::optional<std::string> albumIds;
        if (albumIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(albumIdsQuery.value(), valueQuery_instance)) {
                albumIds = valueQuery_instance;
            }
        }
        auto assetIdsQuery = request.query().get("assetIds");
        std::optional<std::string> assetIds;
        if (assetIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(assetIdsQuery.value(), valueQuery_instance)) {
                assetIds = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto mediaTypeQuery = request.query().get("mediaType");
        std::optional<std::string> mediaType;
        if (mediaTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(mediaTypeQuery.value(), valueQuery_instance)) {
                mediaType = valueQuery_instance;
            }
        }
        auto mimeTypeQuery = request.query().get("mimeType");
        std::optional<std::string> mimeType;
        if (mimeTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(mimeTypeQuery.value(), valueQuery_instance)) {
                mimeType = valueQuery_instance;
            }
        }
        auto keywordQuery = request.query().get("keyword");
        std::optional<std::string> keyword;
        if (keywordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(keywordQuery.value(), valueQuery_instance)) {
                keyword = valueQuery_instance;
            }
        }
        auto versionCodeQuery = request.query().get("versionCode");
        std::optional<int32_t> versionCode;
        if (versionCodeQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(versionCodeQuery.value(), valueQuery_instance)) {
                versionCode = valueQuery_instance;
            }
        }
        auto versionNameQuery = request.query().get("versionName");
        std::optional<std::string> versionName;
        if (versionNameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(versionNameQuery.value(), valueQuery_instance)) {
                versionName = valueQuery_instance;
            }
        }
        auto updatedSinceQuery = request.query().get("updatedSince");
        std::optional<int64_t> updatedSince;
        if (updatedSinceQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(updatedSinceQuery.value(), valueQuery_instance)) {
                updatedSince = valueQuery_instance;
            }
        }
        auto updatedBeforeQuery = request.query().get("updatedBefore");
        std::optional<int64_t> updatedBefore;
        if (updatedBeforeQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(updatedBeforeQuery.value(), valueQuery_instance)) {
                updatedBefore = valueQuery_instance;
            }
        }
        auto sortFieldQuery = request.query().get("sortField");
        std::optional<std::string> sortField;
        if (sortFieldQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(sortFieldQuery.value(), valueQuery_instance)) {
                sortField = valueQuery_instance;
            }
        }
        auto descendingQuery = request.query().get("descending");
        std::optional<bool> descending;
        if (descendingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(descendingQuery.value(), valueQuery_instance)) {
                descending = valueQuery_instance;
            }
        }
        auto searchMediaLibraryQuery = request.query().get("searchMediaLibrary");
        std::optional<bool> searchMediaLibrary;
        if (searchMediaLibraryQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(searchMediaLibraryQuery.value(), valueQuery_instance)) {
                searchMediaLibrary = valueQuery_instance;
            }
        }
        auto filterByBillableQuery = request.query().get("filterByBillable");
        std::optional<bool> filterByBillable;
        if (filterByBillableQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(filterByBillableQuery.value(), valueQuery_instance)) {
                filterByBillable = valueQuery_instance;
            }
        }
        auto activeOnlyQuery = request.query().get("activeOnly");
        std::optional<bool> activeOnly;
        if (activeOnlyQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(activeOnlyQuery.value(), valueQuery_instance)) {
                activeOnly = valueQuery_instance;
            }
        }
        auto returnAppQuery = request.query().get("returnApp");
        std::optional<bool> returnApp;
        if (returnAppQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(returnAppQuery.value(), valueQuery_instance)) {
                returnApp = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto searchModeQuery = request.query().get("searchMode");
        std::optional<std::string> searchMode;
        if (searchModeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(searchModeQuery.value(), valueQuery_instance)) {
                searchMode = valueQuery_instance;
            }
        }
        auto assetTypeQuery = request.query().get("assetType");
        std::optional<std::string> assetType;
        if (assetTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(assetTypeQuery.value(), valueQuery_instance)) {
                assetType = valueQuery_instance;
            }
        }
        auto approvalStatusQuery = request.query().get("approvalStatus");
        std::optional<std::string> approvalStatus;
        if (approvalStatusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(approvalStatusQuery.value(), valueQuery_instance)) {
                approvalStatus = valueQuery_instance;
            }
        }
        auto assignedAccountIdQuery = request.query().get("assignedAccountId");
        std::optional<int64_t> assignedAccountId;
        if (assignedAccountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(assignedAccountIdQuery.value(), valueQuery_instance)) {
                assignedAccountId = valueQuery_instance;
            }
        }
    


        try {





            this->search_assets(version, deviceId, accountId, albumIds, assetIds, appKey, mediaType, mimeType, keyword, versionCode, versionName, updatedSince, updatedBefore, sortField, descending, searchMediaLibrary, filterByBillable, activeOnly, returnApp, start, limit, searchMode, assetType, approvalStatus, assignedAccountId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AssetApi::update_asset_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {
            
            try {
                this->update_asset(request, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void AssetApi::asset_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api


/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "AMQPApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string AMQPApi::base = "";

AMQPApi::AMQPApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void AMQPApi::init() {
    setupRoutes();
}

void AMQPApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Post(*router, base + "/api/:version/queue/consumer/create", Routes::bind(&AMQPApi::consumer_create_handler, this));
    Routes::Post(*router, base + "/api/:version/queue/consumer/update", Routes::bind(&AMQPApi::consumer_update_handler, this));
    Routes::Post(*router, base + "/api/:version/queue/create", Routes::bind(&AMQPApi::queue_create_handler, this));
    Routes::Post(*router, base + "/api/:version/queue/delete", Routes::bind(&AMQPApi::queue_delete_handler, this));
    Routes::Get(*router, base + "/api/:version/queue/get", Routes::bind(&AMQPApi::queue_get_handler, this));
    Routes::Post(*router, base + "/api/:version/queue/publish", Routes::bind(&AMQPApi::queue_publish_handler, this));
    Routes::Get(*router, base + "/api/:version/queue/search", Routes::bind(&AMQPApi::queue_search_handler, this));
    Routes::Post(*router, base + "/api/:version/queue/update", Routes::bind(&AMQPApi::queue_update_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&AMQPApi::amqp_api_default_handler, this));
}

void AMQPApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> AMQPApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void AMQPApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> AMQPApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void AMQPApi::consumer_create_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto hostnameQuery = request.query().get("hostname");
        std::optional<std::string> hostname;
        if (hostnameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(hostnameQuery.value(), valueQuery_instance)) {
                hostname = valueQuery_instance;
            }
        }
        auto portQuery = request.query().get("port");
        std::optional<int32_t> port;
        if (portQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(portQuery.value(), valueQuery_instance)) {
                port = valueQuery_instance;
            }
        }
        auto usernameQuery = request.query().get("username");
        std::optional<std::string> username;
        if (usernameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(usernameQuery.value(), valueQuery_instance)) {
                username = valueQuery_instance;
            }
        }
        auto passwordQuery = request.query().get("password");
        std::optional<std::string> password;
        if (passwordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(passwordQuery.value(), valueQuery_instance)) {
                password = valueQuery_instance;
            }
        }
        auto virtualHostQuery = request.query().get("virtualHost");
        std::optional<std::string> virtualHost;
        if (virtualHostQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(virtualHostQuery.value(), valueQuery_instance)) {
                virtualHost = valueQuery_instance;
            }
        }
        auto exchangerQuery = request.query().get("exchanger");
        std::optional<std::string> exchanger;
        if (exchangerQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(exchangerQuery.value(), valueQuery_instance)) {
                exchanger = valueQuery_instance;
            }
        }
        auto exchangerTypeQuery = request.query().get("exchangerType");
        std::optional<std::string> exchangerType;
        if (exchangerTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(exchangerTypeQuery.value(), valueQuery_instance)) {
                exchangerType = valueQuery_instance;
            }
        }
        auto workersQuery = request.query().get("workers");
        std::optional<int32_t> workers;
        if (workersQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(workersQuery.value(), valueQuery_instance)) {
                workers = valueQuery_instance;
            }
        }
        auto dataMappingQuery = request.query().get("dataMapping");
        std::optional<std::string> dataMapping;
        if (dataMappingQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(dataMappingQuery.value(), valueQuery_instance)) {
                dataMapping = valueQuery_instance;
            }
        }
        auto useSSLQuery = request.query().get("useSSL");
        std::optional<bool> useSSL;
        if (useSSLQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(useSSLQuery.value(), valueQuery_instance)) {
                useSSL = valueQuery_instance;
            }
        }
    


        try {





            this->consumer_create(version, appKey, name, hostname, username, password, dataMapping, deviceId, accountId, port, virtualHost, exchanger, exchangerType, workers, useSSL, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::consumer_update_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
        auto dataMappingQuery = request.query().get("dataMapping");
        std::optional<std::string> dataMapping;
        if (dataMappingQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(dataMappingQuery.value(), valueQuery_instance)) {
                dataMapping = valueQuery_instance;
            }
        }
        auto useSSLQuery = request.query().get("useSSL");
        std::optional<bool> useSSL;
        if (useSSLQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(useSSLQuery.value(), valueQuery_instance)) {
                useSSL = valueQuery_instance;
            }
        }
    


        try {





            this->consumer_update(version, appKey, queueId, dataMapping, deviceId, accountId, useSSL, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_create_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto workersQuery = request.query().get("workers");
        std::optional<int32_t> workers;
        if (workersQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(workersQuery.value(), valueQuery_instance)) {
                workers = valueQuery_instance;
            }
        }
        auto analyticTagsQuery = request.query().get("analyticTags");
        std::optional<std::string> analyticTags;
        if (analyticTagsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(analyticTagsQuery.value(), valueQuery_instance)) {
                analyticTags = valueQuery_instance;
            }
        }
        auto hostnameQuery = request.query().get("hostname");
        std::optional<std::string> hostname;
        if (hostnameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(hostnameQuery.value(), valueQuery_instance)) {
                hostname = valueQuery_instance;
            }
        }
        auto portQuery = request.query().get("port");
        std::optional<int32_t> port;
        if (portQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(portQuery.value(), valueQuery_instance)) {
                port = valueQuery_instance;
            }
        }
        auto usernameQuery = request.query().get("username");
        std::optional<std::string> username;
        if (usernameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(usernameQuery.value(), valueQuery_instance)) {
                username = valueQuery_instance;
            }
        }
        auto passwordQuery = request.query().get("password");
        std::optional<std::string> password;
        if (passwordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(passwordQuery.value(), valueQuery_instance)) {
                password = valueQuery_instance;
            }
        }
        auto virtualHostQuery = request.query().get("virtualHost");
        std::optional<std::string> virtualHost;
        if (virtualHostQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(virtualHostQuery.value(), valueQuery_instance)) {
                virtualHost = valueQuery_instance;
            }
        }
        auto useSSLQuery = request.query().get("useSSL");
        std::optional<bool> useSSL;
        if (useSSLQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(useSSLQuery.value(), valueQuery_instance)) {
                useSSL = valueQuery_instance;
            }
        }
    


        try {





            this->queue_create(version, appKey, name, deviceId, accountId, workers, analyticTags, hostname, port, username, password, virtualHost, useSSL, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_delete_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
    


        try {





            this->queue_delete(version, queueId, deviceId, accountId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_get_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto hostnameQuery = request.query().get("hostname");
        std::optional<std::string> hostname;
        if (hostnameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(hostnameQuery.value(), valueQuery_instance)) {
                hostname = valueQuery_instance;
            }
        }
        auto virtualHostQuery = request.query().get("virtualHost");
        std::optional<std::string> virtualHost;
        if (virtualHostQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(virtualHostQuery.value(), valueQuery_instance)) {
                virtualHost = valueQuery_instance;
            }
        }
    


        try {





            this->queue_get(version, deviceId, accountId, queueId, appKey, name, hostname, virtualHost, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_publish_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto hostnameQuery = request.query().get("hostname");
        std::optional<std::string> hostname;
        if (hostnameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(hostnameQuery.value(), valueQuery_instance)) {
                hostname = valueQuery_instance;
            }
        }
        auto virtualHostQuery = request.query().get("virtualHost");
        std::optional<std::string> virtualHost;
        if (virtualHostQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(virtualHostQuery.value(), valueQuery_instance)) {
                virtualHost = valueQuery_instance;
            }
        }
        auto messageQuery = request.query().get("message");
        std::optional<std::string> message;
        if (messageQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(messageQuery.value(), valueQuery_instance)) {
                message = valueQuery_instance;
            }
        }
    


        try {





            this->queue_publish(version, message, queueId, appKey, name, hostname, virtualHost, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_search_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
    


        try {





            this->queue_search(version, queueId, deviceId, accountId, name, start, limit, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void AMQPApi::queue_update_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto queueIdQuery = request.query().get("queueId");
        std::optional<int64_t> queueId;
        if (queueIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(queueIdQuery.value(), valueQuery_instance)) {
                queueId = valueQuery_instance;
            }
        }
        auto workersQuery = request.query().get("workers");
        std::optional<int32_t> workers;
        if (workersQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(workersQuery.value(), valueQuery_instance)) {
                workers = valueQuery_instance;
            }
        }
        auto analyticTagsQuery = request.query().get("analyticTags");
        std::optional<std::string> analyticTags;
        if (analyticTagsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(analyticTagsQuery.value(), valueQuery_instance)) {
                analyticTags = valueQuery_instance;
            }
        }
        auto hostnameQuery = request.query().get("hostname");
        std::optional<std::string> hostname;
        if (hostnameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(hostnameQuery.value(), valueQuery_instance)) {
                hostname = valueQuery_instance;
            }
        }
        auto portQuery = request.query().get("port");
        std::optional<int32_t> port;
        if (portQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(portQuery.value(), valueQuery_instance)) {
                port = valueQuery_instance;
            }
        }
        auto usernameQuery = request.query().get("username");
        std::optional<std::string> username;
        if (usernameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(usernameQuery.value(), valueQuery_instance)) {
                username = valueQuery_instance;
            }
        }
        auto passwordQuery = request.query().get("password");
        std::optional<std::string> password;
        if (passwordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(passwordQuery.value(), valueQuery_instance)) {
                password = valueQuery_instance;
            }
        }
        auto virtualHostQuery = request.query().get("virtualHost");
        std::optional<std::string> virtualHost;
        if (virtualHostQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(virtualHostQuery.value(), valueQuery_instance)) {
                virtualHost = valueQuery_instance;
            }
        }
        auto useSSLQuery = request.query().get("useSSL");
        std::optional<bool> useSSL;
        if (useSSLQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(useSSLQuery.value(), valueQuery_instance)) {
                useSSL = valueQuery_instance;
            }
        }
    


        try {





            this->queue_update(version, queueId, deviceId, accountId, appKey, workers, analyticTags, hostname, port, username, password, virtualHost, useSSL, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void AMQPApi::amqp_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api


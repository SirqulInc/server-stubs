/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "MissionApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string MissionApi::base = "";

MissionApi::MissionApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void MissionApi::init() {
    setupRoutes();
}

void MissionApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Post(*router, base + "/api/:version/mission/create", Routes::bind(&MissionApi::create_mission_handler, this));
    Routes::Post(*router, base + "/api/:version/mission/delete", Routes::bind(&MissionApi::delete_mission_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/find", Routes::bind(&MissionApi::find_missions_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/get", Routes::bind(&MissionApi::get_mission_handler, this));
    Routes::Post(*router, base + "/api/:version/mission/import", Routes::bind(&MissionApi::import_mission_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/format/search", Routes::bind(&MissionApi::search_mission_formats_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/search", Routes::bind(&MissionApi::search_missions_handler, this));
    Routes::Get(*router, base + "/api/:version/mission/searchByBillableEntity", Routes::bind(&MissionApi::search_missions_by_billable_entity_handler, this));
    Routes::Post(*router, base + "/api/:version/mission/update", Routes::bind(&MissionApi::update_mission_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&MissionApi::mission_api_default_handler, this));
}

void MissionApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> MissionApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void MissionApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> MissionApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void MissionApi::create_mission_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto titleQuery = request.query().get("title");
        std::optional<std::string> title;
        if (titleQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(titleQuery.value(), valueQuery_instance)) {
                title = valueQuery_instance;
            }
        }
        auto descriptionQuery = request.query().get("description");
        std::optional<std::string> description;
        if (descriptionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(descriptionQuery.value(), valueQuery_instance)) {
                description = valueQuery_instance;
            }
        }
        auto subTypeQuery = request.query().get("subType");
        std::optional<std::string> subType;
        if (subTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(subTypeQuery.value(), valueQuery_instance)) {
                subType = valueQuery_instance;
            }
        }
        auto startDateQuery = request.query().get("startDate");
        std::optional<int64_t> startDate;
        if (startDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(startDateQuery.value(), valueQuery_instance)) {
                startDate = valueQuery_instance;
            }
        }
        auto endDateQuery = request.query().get("endDate");
        std::optional<int64_t> endDate;
        if (endDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(endDateQuery.value(), valueQuery_instance)) {
                endDate = valueQuery_instance;
            }
        }
        auto activeQuery = request.query().get("active");
        std::optional<bool> active;
        if (activeQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(activeQuery.value(), valueQuery_instance)) {
                active = valueQuery_instance;
            }
        }
        auto gameLevelIdsQuery = request.query().get("gameLevelIds");
        std::optional<std::string> gameLevelIds;
        if (gameLevelIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(gameLevelIdsQuery.value(), valueQuery_instance)) {
                gameLevelIds = valueQuery_instance;
            }
        }
        auto creativeIdsQuery = request.query().get("creativeIds");
        std::optional<std::string> creativeIds;
        if (creativeIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(creativeIdsQuery.value(), valueQuery_instance)) {
                creativeIds = valueQuery_instance;
            }
        }
        auto audienceIdsQuery = request.query().get("audienceIds");
        std::optional<std::string> audienceIds;
        if (audienceIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(audienceIdsQuery.value(), valueQuery_instance)) {
                audienceIds = valueQuery_instance;
            }
        }
        auto missionTaskQuery = request.query().get("missionTask");
        std::optional<std::string> missionTask;
        if (missionTaskQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(missionTaskQuery.value(), valueQuery_instance)) {
                missionTask = valueQuery_instance;
            }
        }
        auto formatTypeQuery = request.query().get("formatType");
        std::optional<std::string> formatType;
        if (formatTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(formatTypeQuery.value(), valueQuery_instance)) {
                formatType = valueQuery_instance;
            }
        }
        auto offerIdQuery = request.query().get("offerId");
        std::optional<int64_t> offerId;
        if (offerIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(offerIdQuery.value(), valueQuery_instance)) {
                offerId = valueQuery_instance;
            }
        }
        auto balanceQuery = request.query().get("balance");
        std::optional<double> balance;
        if (balanceQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(balanceQuery.value(), valueQuery_instance)) {
                balance = valueQuery_instance;
            }
        }
        auto advancedReportingQuery = request.query().get("advancedReporting");
        std::optional<bool> advancedReporting;
        if (advancedReportingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(advancedReportingQuery.value(), valueQuery_instance)) {
                advancedReporting = valueQuery_instance;
            }
        }
        auto allocateTicketsQuery = request.query().get("allocateTickets");
        std::optional<bool> allocateTickets;
        if (allocateTicketsQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(allocateTicketsQuery.value(), valueQuery_instance)) {
                allocateTickets = valueQuery_instance;
            }
        }
        auto ticketCountQuery = request.query().get("ticketCount");
        std::optional<int64_t> ticketCount;
        if (ticketCountQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(ticketCountQuery.value(), valueQuery_instance)) {
                ticketCount = valueQuery_instance;
            }
        }
        auto ticketTypeQuery = request.query().get("ticketType");
        std::optional<std::string> ticketType;
        if (ticketTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(ticketTypeQuery.value(), valueQuery_instance)) {
                ticketType = valueQuery_instance;
            }
        }
        auto pointsQuery = request.query().get("points");
        std::optional<int64_t> points;
        if (pointsQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(pointsQuery.value(), valueQuery_instance)) {
                points = valueQuery_instance;
            }
        }
        auto metaDataQuery = request.query().get("metaData");
        std::optional<std::string> metaData;
        if (metaDataQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(metaDataQuery.value(), valueQuery_instance)) {
                metaData = valueQuery_instance;
            }
        }
        auto applicationIdsQuery = request.query().get("applicationIds");
        std::optional<std::string> applicationIds;
        if (applicationIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(applicationIdsQuery.value(), valueQuery_instance)) {
                applicationIds = valueQuery_instance;
            }
        }
        auto devicesQuery = request.query().get("devices");
        std::optional<std::string> devices;
        if (devicesQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(devicesQuery.value(), valueQuery_instance)) {
                devices = valueQuery_instance;
            }
        }
        auto deviceIdsQuery = request.query().get("deviceIds");
        std::optional<std::string> deviceIds;
        if (deviceIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdsQuery.value(), valueQuery_instance)) {
                deviceIds = valueQuery_instance;
            }
        }
        auto deviceVersionsQuery = request.query().get("deviceVersions");
        std::optional<std::string> deviceVersions;
        if (deviceVersionsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceVersionsQuery.value(), valueQuery_instance)) {
                deviceVersions = valueQuery_instance;
            }
        }
        auto locationsQuery = request.query().get("locations");
        std::optional<std::string> locations;
        if (locationsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(locationsQuery.value(), valueQuery_instance)) {
                locations = valueQuery_instance;
            }
        }
        auto radiusQuery = request.query().get("radius");
        std::optional<std::string> radius;
        if (radiusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(radiusQuery.value(), valueQuery_instance)) {
                radius = valueQuery_instance;
            }
        }
    


        try {





            this->create_mission(version, accountId, title, description, subType, startDate, endDate, active, gameLevelIds, creativeIds, audienceIds, missionTask, formatType, offerId, balance, advancedReporting, allocateTickets, ticketCount, ticketType, points, metaData, applicationIds, devices, deviceIds, deviceVersions, locations, radius, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::delete_mission_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
    


        try {





            this->delete_mission(version, accountId, missionId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::find_missions_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto suffixQuery = request.query().get("suffix");
        std::optional<std::string> suffix;
        if (suffixQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(suffixQuery.value(), valueQuery_instance)) {
                suffix = valueQuery_instance;
            }
        }
        auto typeQuery = request.query().get("type");
        std::optional<std::string> type;
        if (typeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(typeQuery.value(), valueQuery_instance)) {
                type = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appVersionQuery = request.query().get("appVersion");
        std::optional<std::string> appVersion;
        if (appVersionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appVersionQuery.value(), valueQuery_instance)) {
                appVersion = valueQuery_instance;
            }
        }
        auto latitudeQuery = request.query().get("latitude");
        std::optional<double> latitude;
        if (latitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(latitudeQuery.value(), valueQuery_instance)) {
                latitude = valueQuery_instance;
            }
        }
        auto longitudeQuery = request.query().get("longitude");
        std::optional<double> longitude;
        if (longitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(longitudeQuery.value(), valueQuery_instance)) {
                longitude = valueQuery_instance;
            }
        }
        auto deviceQuery = request.query().get("device");
        std::optional<std::string> device;
        if (deviceQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceQuery.value(), valueQuery_instance)) {
                device = valueQuery_instance;
            }
        }
        auto deviceIdentifierQuery = request.query().get("deviceIdentifier");
        std::optional<int64_t> deviceIdentifier;
        if (deviceIdentifierQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(deviceIdentifierQuery.value(), valueQuery_instance)) {
                deviceIdentifier = valueQuery_instance;
            }
        }
        auto deviceVersionQuery = request.query().get("deviceVersion");
        std::optional<std::string> deviceVersion;
        if (deviceVersionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceVersionQuery.value(), valueQuery_instance)) {
                deviceVersion = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
        auto includeAudiencesQuery = request.query().get("includeAudiences");
        std::optional<bool> includeAudiences;
        if (includeAudiencesQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeAudiencesQuery.value(), valueQuery_instance)) {
                includeAudiences = valueQuery_instance;
            }
        }
        auto allocatesTicketsQuery = request.query().get("allocatesTickets");
        std::optional<bool> allocatesTickets;
        if (allocatesTicketsQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(allocatesTicketsQuery.value(), valueQuery_instance)) {
                allocatesTickets = valueQuery_instance;
            }
        }
        auto randomizeQuery = request.query().get("randomize");
        std::optional<bool> randomize;
        if (randomizeQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(randomizeQuery.value(), valueQuery_instance)) {
                randomize = valueQuery_instance;
            }
        }
        auto targetedAdsOnlyQuery = request.query().get("targetedAdsOnly");
        std::optional<bool> targetedAdsOnly;
        if (targetedAdsOnlyQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(targetedAdsOnlyQuery.value(), valueQuery_instance)) {
                targetedAdsOnly = valueQuery_instance;
            }
        }
        auto missionIdsQuery = request.query().get("missionIds");
        std::optional<std::string> missionIds;
        if (missionIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(missionIdsQuery.value(), valueQuery_instance)) {
                missionIds = valueQuery_instance;
            }
        }
        auto audienceOperatorQuery = request.query().get("audienceOperator");
        std::optional<std::string> audienceOperator;
        if (audienceOperatorQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(audienceOperatorQuery.value(), valueQuery_instance)) {
                audienceOperator = valueQuery_instance;
            }
        }
    


        try {





            this->find_missions(version, appKey, suffix, type, accountId, appVersion, latitude, longitude, device, deviceIdentifier, deviceVersion, start, limit, includeGameData, includeAudiences, allocatesTickets, randomize, targetedAdsOnly, missionIds, audienceOperator, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::get_mission_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto returnCreativeQuery = request.query().get("returnCreative");
        std::optional<bool> returnCreative;
        if (returnCreativeQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(returnCreativeQuery.value(), valueQuery_instance)) {
                returnCreative = valueQuery_instance;
            }
        }
    


        try {





            this->get_mission(version, accountId, missionId, returnCreative, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::import_mission_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto latitudeQuery = request.query().get("latitude");
        std::optional<double> latitude;
        if (latitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(latitudeQuery.value(), valueQuery_instance)) {
                latitude = valueQuery_instance;
            }
        }
        auto longitudeQuery = request.query().get("longitude");
        std::optional<double> longitude;
        if (longitudeQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(longitudeQuery.value(), valueQuery_instance)) {
                longitude = valueQuery_instance;
            }
        }
        auto keywordQuery = request.query().get("keyword");
        std::optional<std::string> keyword;
        if (keywordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(keywordQuery.value(), valueQuery_instance)) {
                keyword = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto adSizeQuery = request.query().get("adSize");
        std::optional<std::string> adSize;
        if (adSizeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(adSizeQuery.value(), valueQuery_instance)) {
                adSize = valueQuery_instance;
            }
        }
    


        try {





            this->import_mission(version, accountId, latitude, longitude, appKey, keyword, start, limit, adSize, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::search_mission_formats_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto activeOnlyQuery = request.query().get("activeOnly");
        std::optional<bool> activeOnly;
        if (activeOnlyQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(activeOnlyQuery.value(), valueQuery_instance)) {
                activeOnly = valueQuery_instance;
            }
        }
    


        try {





            this->search_mission_formats(version, start, limit, activeOnly, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::search_missions_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto keywordQuery = request.query().get("keyword");
        std::optional<std::string> keyword;
        if (keywordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(keywordQuery.value(), valueQuery_instance)) {
                keyword = valueQuery_instance;
            }
        }
        auto subTypeQuery = request.query().get("subType");
        std::optional<std::string> subType;
        if (subTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(subTypeQuery.value(), valueQuery_instance)) {
                subType = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
        auto includeAudiencesQuery = request.query().get("includeAudiences");
        std::optional<bool> includeAudiences;
        if (includeAudiencesQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeAudiencesQuery.value(), valueQuery_instance)) {
                includeAudiences = valueQuery_instance;
            }
        }
        auto includeInactiveQuery = request.query().get("includeInactive");
        std::optional<bool> includeInactive;
        if (includeInactiveQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeInactiveQuery.value(), valueQuery_instance)) {
                includeInactive = valueQuery_instance;
            }
        }
        auto suffixQuery = request.query().get("suffix");
        std::optional<std::string> suffix;
        if (suffixQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(suffixQuery.value(), valueQuery_instance)) {
                suffix = valueQuery_instance;
            }
        }
        auto sortFieldQuery = request.query().get("sortField");
        std::optional<std::string> sortField;
        if (sortFieldQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(sortFieldQuery.value(), valueQuery_instance)) {
                sortField = valueQuery_instance;
            }
        }
        auto descendingQuery = request.query().get("descending");
        std::optional<bool> descending;
        if (descendingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(descendingQuery.value(), valueQuery_instance)) {
                descending = valueQuery_instance;
            }
        }
    


        try {





            this->search_missions(version, accountId, keyword, subType, start, limit, includeGameData, includeAudiences, includeInactive, suffix, sortField, descending, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::search_missions_by_billable_entity_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto keywordQuery = request.query().get("keyword");
        std::optional<std::string> keyword;
        if (keywordQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(keywordQuery.value(), valueQuery_instance)) {
                keyword = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto includeGameDataQuery = request.query().get("includeGameData");
        std::optional<bool> includeGameData;
        if (includeGameDataQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeGameDataQuery.value(), valueQuery_instance)) {
                includeGameData = valueQuery_instance;
            }
        }
        auto includeAudiencesQuery = request.query().get("includeAudiences");
        std::optional<bool> includeAudiences;
        if (includeAudiencesQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeAudiencesQuery.value(), valueQuery_instance)) {
                includeAudiences = valueQuery_instance;
            }
        }
        auto includeInactiveQuery = request.query().get("includeInactive");
        std::optional<bool> includeInactive;
        if (includeInactiveQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(includeInactiveQuery.value(), valueQuery_instance)) {
                includeInactive = valueQuery_instance;
            }
        }
        auto suffixQuery = request.query().get("suffix");
        std::optional<std::string> suffix;
        if (suffixQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(suffixQuery.value(), valueQuery_instance)) {
                suffix = valueQuery_instance;
            }
        }
        auto sortFieldQuery = request.query().get("sortField");
        std::optional<std::string> sortField;
        if (sortFieldQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(sortFieldQuery.value(), valueQuery_instance)) {
                sortField = valueQuery_instance;
            }
        }
        auto descendingQuery = request.query().get("descending");
        std::optional<bool> descending;
        if (descendingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(descendingQuery.value(), valueQuery_instance)) {
                descending = valueQuery_instance;
            }
        }
    


        try {





            this->search_missions_by_billable_entity(version, accountId, keyword, start, limit, includeGameData, includeAudiences, includeInactive, suffix, sortField, descending, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void MissionApi::update_mission_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto missionIdQuery = request.query().get("missionId");
        std::optional<int64_t> missionId;
        if (missionIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(missionIdQuery.value(), valueQuery_instance)) {
                missionId = valueQuery_instance;
            }
        }
        auto titleQuery = request.query().get("title");
        std::optional<std::string> title;
        if (titleQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(titleQuery.value(), valueQuery_instance)) {
                title = valueQuery_instance;
            }
        }
        auto descriptionQuery = request.query().get("description");
        std::optional<std::string> description;
        if (descriptionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(descriptionQuery.value(), valueQuery_instance)) {
                description = valueQuery_instance;
            }
        }
        auto subTypeQuery = request.query().get("subType");
        std::optional<std::string> subType;
        if (subTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(subTypeQuery.value(), valueQuery_instance)) {
                subType = valueQuery_instance;
            }
        }
        auto metaDataQuery = request.query().get("metaData");
        std::optional<std::string> metaData;
        if (metaDataQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(metaDataQuery.value(), valueQuery_instance)) {
                metaData = valueQuery_instance;
            }
        }
        auto startDateQuery = request.query().get("startDate");
        std::optional<int64_t> startDate;
        if (startDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(startDateQuery.value(), valueQuery_instance)) {
                startDate = valueQuery_instance;
            }
        }
        auto endDateQuery = request.query().get("endDate");
        std::optional<int64_t> endDate;
        if (endDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(endDateQuery.value(), valueQuery_instance)) {
                endDate = valueQuery_instance;
            }
        }
        auto activeQuery = request.query().get("active");
        std::optional<bool> active;
        if (activeQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(activeQuery.value(), valueQuery_instance)) {
                active = valueQuery_instance;
            }
        }
        auto gameLevelIdsQuery = request.query().get("gameLevelIds");
        std::optional<std::string> gameLevelIds;
        if (gameLevelIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(gameLevelIdsQuery.value(), valueQuery_instance)) {
                gameLevelIds = valueQuery_instance;
            }
        }
        auto creativeIdsQuery = request.query().get("creativeIds");
        std::optional<std::string> creativeIds;
        if (creativeIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(creativeIdsQuery.value(), valueQuery_instance)) {
                creativeIds = valueQuery_instance;
            }
        }
        auto audienceIdsQuery = request.query().get("audienceIds");
        std::optional<std::string> audienceIds;
        if (audienceIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(audienceIdsQuery.value(), valueQuery_instance)) {
                audienceIds = valueQuery_instance;
            }
        }
        auto offerIdQuery = request.query().get("offerId");
        std::optional<int64_t> offerId;
        if (offerIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(offerIdQuery.value(), valueQuery_instance)) {
                offerId = valueQuery_instance;
            }
        }
        auto balanceQuery = request.query().get("balance");
        std::optional<double> balance;
        if (balanceQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(balanceQuery.value(), valueQuery_instance)) {
                balance = valueQuery_instance;
            }
        }
        auto advancedReportingQuery = request.query().get("advancedReporting");
        std::optional<bool> advancedReporting;
        if (advancedReportingQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(advancedReportingQuery.value(), valueQuery_instance)) {
                advancedReporting = valueQuery_instance;
            }
        }
        auto allocateTicketsQuery = request.query().get("allocateTickets");
        std::optional<bool> allocateTickets;
        if (allocateTicketsQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(allocateTicketsQuery.value(), valueQuery_instance)) {
                allocateTickets = valueQuery_instance;
            }
        }
        auto ticketCountQuery = request.query().get("ticketCount");
        std::optional<int64_t> ticketCount;
        if (ticketCountQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(ticketCountQuery.value(), valueQuery_instance)) {
                ticketCount = valueQuery_instance;
            }
        }
        auto ticketTypeQuery = request.query().get("ticketType");
        std::optional<std::string> ticketType;
        if (ticketTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(ticketTypeQuery.value(), valueQuery_instance)) {
                ticketType = valueQuery_instance;
            }
        }
        auto pointsQuery = request.query().get("points");
        std::optional<int64_t> points;
        if (pointsQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(pointsQuery.value(), valueQuery_instance)) {
                points = valueQuery_instance;
            }
        }
        auto applicationIdsQuery = request.query().get("applicationIds");
        std::optional<std::string> applicationIds;
        if (applicationIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(applicationIdsQuery.value(), valueQuery_instance)) {
                applicationIds = valueQuery_instance;
            }
        }
        auto devicesQuery = request.query().get("devices");
        std::optional<std::string> devices;
        if (devicesQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(devicesQuery.value(), valueQuery_instance)) {
                devices = valueQuery_instance;
            }
        }
        auto deviceIdsQuery = request.query().get("deviceIds");
        std::optional<std::string> deviceIds;
        if (deviceIdsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdsQuery.value(), valueQuery_instance)) {
                deviceIds = valueQuery_instance;
            }
        }
        auto deviceVersionsQuery = request.query().get("deviceVersions");
        std::optional<std::string> deviceVersions;
        if (deviceVersionsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceVersionsQuery.value(), valueQuery_instance)) {
                deviceVersions = valueQuery_instance;
            }
        }
        auto locationsQuery = request.query().get("locations");
        std::optional<std::string> locations;
        if (locationsQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(locationsQuery.value(), valueQuery_instance)) {
                locations = valueQuery_instance;
            }
        }
        auto radiusQuery = request.query().get("radius");
        std::optional<std::string> radius;
        if (radiusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(radiusQuery.value(), valueQuery_instance)) {
                radius = valueQuery_instance;
            }
        }
    


        try {





            this->update_mission(version, accountId, missionId, title, description, subType, metaData, startDate, endDate, active, gameLevelIds, creativeIds, audienceIds, offerId, balance, advancedReporting, allocateTickets, ticketCount, ticketType, points, applicationIds, devices, deviceIds, deviceVersions, locations, radius, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void MissionApi::mission_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api


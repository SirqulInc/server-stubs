/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "ReportingApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string ReportingApi::base = "";

ReportingApi::ReportingApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void ReportingApi::init() {
    setupRoutes();
}

void ReportingApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Post(*router, base + "/api/:version/report/batch/create", Routes::bind(&ReportingApi::create_batch_handler, this));
    Routes::Post(*router, base + "/api/:version/report/region/summary/batch", Routes::bind(&ReportingApi::create_region_leg_summary_batch_handler, this));
    Routes::Post(*router, base + "/api/:version/report/batch/delete", Routes::bind(&ReportingApi::delete_batch_handler, this));
    Routes::Get(*router, base + "/api/:version/report/batch/get", Routes::bind(&ReportingApi::get_report_batch_handler, this));
    Routes::Post(*router, base + "/api/:version/report/run", Routes::bind(&ReportingApi::run_report_handler, this));
    Routes::Get(*router, base + "/api/:version/report/batch/search", Routes::bind(&ReportingApi::search_batch_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&ReportingApi::reporting_api_default_handler, this));
}

void ReportingApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> ReportingApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void ReportingApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> ReportingApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void ReportingApi::create_batch_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto statusQuery = request.query().get("status");
        std::optional<std::string> status;
        if (statusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(statusQuery.value(), valueQuery_instance)) {
                status = valueQuery_instance;
            }
        }
        auto endpointQuery = request.query().get("endpoint");
        std::optional<std::string> endpoint;
        if (endpointQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(endpointQuery.value(), valueQuery_instance)) {
                endpoint = valueQuery_instance;
            }
        }
        auto parametersQuery = request.query().get("parameters");
        std::optional<std::string> parameters;
        if (parametersQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(parametersQuery.value(), valueQuery_instance)) {
                parameters = valueQuery_instance;
            }
        }
        auto nameQuery = request.query().get("name");
        std::optional<std::string> name;
        if (nameQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(nameQuery.value(), valueQuery_instance)) {
                name = valueQuery_instance;
            }
        }
        auto startDateQuery = request.query().get("startDate");
        std::optional<int64_t> startDate;
        if (startDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(startDateQuery.value(), valueQuery_instance)) {
                startDate = valueQuery_instance;
            }
        }
        auto endDateQuery = request.query().get("endDate");
        std::optional<int64_t> endDate;
        if (endDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(endDateQuery.value(), valueQuery_instance)) {
                endDate = valueQuery_instance;
            }
        }
        auto descriptionQuery = request.query().get("description");
        std::optional<std::string> description;
        if (descriptionQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(descriptionQuery.value(), valueQuery_instance)) {
                description = valueQuery_instance;
            }
        }
        auto previewLimitQuery = request.query().get("previewLimit");
        std::optional<int32_t> previewLimit;
        if (previewLimitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(previewLimitQuery.value(), valueQuery_instance)) {
                previewLimit = valueQuery_instance;
            }
        }
        auto pageUrlQuery = request.query().get("pageUrl");
        std::optional<std::string> pageUrl;
        if (pageUrlQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(pageUrlQuery.value(), valueQuery_instance)) {
                pageUrl = valueQuery_instance;
            }
        }
    


        try {





            this->create_batch(version, accountId, status, previewLimit, appKey, endpoint, parameters, name, startDate, endDate, description, pageUrl, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void ReportingApi::create_region_leg_summary_batch_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        // Getting the body param
                std::vector<RegionLegSummary> body;
        
        
    

        try {
            nlohmann::json::parse(request.body()).get_to(body);
            for (const auto& validationParam : body) 
                validationParam.validate();
        } catch (std::exception& e) {
            this->handleParsingException(e, response);
            return;
        }

        try {





            this->create_region_leg_summary_batch(version, body, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void ReportingApi::delete_batch_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto batchIdQuery = request.query().get("batchId");
        std::optional<int64_t> batchId;
        if (batchIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(batchIdQuery.value(), valueQuery_instance)) {
                batchId = valueQuery_instance;
            }
        }
    


        try {





            this->delete_batch(version, accountId, batchId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void ReportingApi::get_report_batch_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto batchIdQuery = request.query().get("batchId");
        std::optional<int64_t> batchId;
        if (batchIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(batchIdQuery.value(), valueQuery_instance)) {
                batchId = valueQuery_instance;
            }
        }
        auto allResultsQuery = request.query().get("allResults");
        std::optional<bool> allResults;
        if (allResultsQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(allResultsQuery.value(), valueQuery_instance)) {
                allResults = valueQuery_instance;
            }
        }
    


        try {





            this->get_report_batch(version, accountId, batchId, allResults, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void ReportingApi::run_report_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto queryQuery = request.query().get("query");
        std::optional<std::string> query;
        if (queryQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(queryQuery.value(), valueQuery_instance)) {
                query = valueQuery_instance;
            }
        }
        auto parametersQuery = request.query().get("parameters");
        std::optional<std::string> parameters;
        if (parametersQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(parametersQuery.value(), valueQuery_instance)) {
                parameters = valueQuery_instance;
            }
        }
        auto orderQuery = request.query().get("order");
        std::optional<std::string> order;
        if (orderQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(orderQuery.value(), valueQuery_instance)) {
                order = valueQuery_instance;
            }
        }
        auto descQuery = request.query().get("desc");
        std::optional<bool> desc;
        if (descQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(descQuery.value(), valueQuery_instance)) {
                desc = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int64_t> start;
        if (startQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int64_t> limit;
        if (limitQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
        auto responseFormatQuery = request.query().get("responseFormat");
        std::optional<std::string> responseFormat;
        if (responseFormatQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(responseFormatQuery.value(), valueQuery_instance)) {
                responseFormat = valueQuery_instance;
            }
        }
    


        try {





            this->run_report(version, desc, accountId, query, parameters, order, start, limit, responseFormat, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void ReportingApi::search_batch_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto namesQuery = request.query().get("names");
        std::optional<std::string> names;
        if (namesQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(namesQuery.value(), valueQuery_instance)) {
                names = valueQuery_instance;
            }
        }
        auto appKeyQuery = request.query().get("appKey");
        std::optional<std::string> appKey;
        if (appKeyQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(appKeyQuery.value(), valueQuery_instance)) {
                appKey = valueQuery_instance;
            }
        }
        auto statusQuery = request.query().get("status");
        std::optional<std::string> status;
        if (statusQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(statusQuery.value(), valueQuery_instance)) {
                status = valueQuery_instance;
            }
        }
        auto globalAppSearchQuery = request.query().get("globalAppSearch");
        std::optional<bool> globalAppSearch;
        if (globalAppSearchQuery.has_value()) {
            bool valueQuery_instance;
            if (fromStringValue(globalAppSearchQuery.value(), valueQuery_instance)) {
                globalAppSearch = valueQuery_instance;
            }
        }
        auto startDateQuery = request.query().get("startDate");
        std::optional<int64_t> startDate;
        if (startDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(startDateQuery.value(), valueQuery_instance)) {
                startDate = valueQuery_instance;
            }
        }
        auto endDateQuery = request.query().get("endDate");
        std::optional<int64_t> endDate;
        if (endDateQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(endDateQuery.value(), valueQuery_instance)) {
                endDate = valueQuery_instance;
            }
        }
        auto startQuery = request.query().get("start");
        std::optional<int32_t> start;
        if (startQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(startQuery.value(), valueQuery_instance)) {
                start = valueQuery_instance;
            }
        }
        auto limitQuery = request.query().get("limit");
        std::optional<int32_t> limit;
        if (limitQuery.has_value()) {
            int32_t valueQuery_instance;
            if (fromStringValue(limitQuery.value(), valueQuery_instance)) {
                limit = valueQuery_instance;
            }
        }
    


        try {





            this->search_batch(version, accountId, start, limit, names, appKey, status, globalAppSearch, startDate, endDate, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void ReportingApi::reporting_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api


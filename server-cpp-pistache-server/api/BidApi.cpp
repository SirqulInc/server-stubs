/**
* Sirqul IoT Platform
* Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
*
* The version of the OpenAPI document: 3.16
* Contact: info@sirqul.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "BidApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string BidApi::base = "";

BidApi::BidApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{}

void BidApi::init() {
    setupRoutes();
}

void BidApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Post(*router, base + "/api/:version/bid/create", Routes::bind(&BidApi::create_bid_handler, this));
    Routes::Post(*router, base + "/api/:version/bid/delete", Routes::bind(&BidApi::delete_bid_handler, this));
    Routes::Get(*router, base + "/api/:version/bid/get", Routes::bind(&BidApi::get_bid_handler, this));
    Routes::Post(*router, base + "/api/:version/bid/update", Routes::bind(&BidApi::update_bid_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&BidApi::bid_api_default_handler, this));
}

void BidApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> BidApi::handleParsingException(const std::exception& ex) const noexcept {
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void BidApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept {
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> BidApi::handleOperationException(const std::exception& ex) const noexcept {
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void BidApi::create_bid_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto biddableTypeQuery = request.query().get("biddableType");
        std::optional<std::string> biddableType;
        if (biddableTypeQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(biddableTypeQuery.value(), valueQuery_instance)) {
                biddableType = valueQuery_instance;
            }
        }
        auto biddableIdQuery = request.query().get("biddableId");
        std::optional<int64_t> biddableId;
        if (biddableIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(biddableIdQuery.value(), valueQuery_instance)) {
                biddableId = valueQuery_instance;
            }
        }
        auto amountPerViewQuery = request.query().get("amountPerView");
        std::optional<double> amountPerView;
        if (amountPerViewQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(amountPerViewQuery.value(), valueQuery_instance)) {
                amountPerView = valueQuery_instance;
            }
        }
        auto amountPerActionQuery = request.query().get("amountPerAction");
        std::optional<double> amountPerAction;
        if (amountPerActionQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(amountPerActionQuery.value(), valueQuery_instance)) {
                amountPerAction = valueQuery_instance;
            }
        }
        auto budgetAmountQuery = request.query().get("budgetAmount");
        std::optional<double> budgetAmount;
        if (budgetAmountQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(budgetAmountQuery.value(), valueQuery_instance)) {
                budgetAmount = valueQuery_instance;
            }
        }
        auto budgetScheduleQuery = request.query().get("budgetSchedule");
        std::optional<std::string> budgetSchedule;
        if (budgetScheduleQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(budgetScheduleQuery.value(), valueQuery_instance)) {
                budgetSchedule = valueQuery_instance;
            }
        }
    


        try {





            this->create_bid(version, biddableType, biddableId, amountPerView, amountPerAction, budgetAmount, budgetSchedule, deviceId, accountId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void BidApi::delete_bid_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto bidIdQuery = request.query().get("bidId");
        std::optional<int64_t> bidId;
        if (bidIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(bidIdQuery.value(), valueQuery_instance)) {
                bidId = valueQuery_instance;
            }
        }
    


        try {





            this->delete_bid(version, bidId, deviceId, accountId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void BidApi::get_bid_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto bidIdQuery = request.query().get("bidId");
        std::optional<int64_t> bidId;
        if (bidIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(bidIdQuery.value(), valueQuery_instance)) {
                bidId = valueQuery_instance;
            }
        }
    


        try {





            this->get_bid(version, bidId, deviceId, accountId, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}

void BidApi::update_bid_handler(const Pistache::Rest::Request& request, Pistache::Http::ResponseWriter response) {
    try {

        // Getting the path params
        auto version = request.param(":version").as<double>();
        
        
        // Getting the query params
        auto deviceIdQuery = request.query().get("deviceId");
        std::optional<std::string> deviceId;
        if (deviceIdQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(deviceIdQuery.value(), valueQuery_instance)) {
                deviceId = valueQuery_instance;
            }
        }
        auto accountIdQuery = request.query().get("accountId");
        std::optional<int64_t> accountId;
        if (accountIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(accountIdQuery.value(), valueQuery_instance)) {
                accountId = valueQuery_instance;
            }
        }
        auto bidIdQuery = request.query().get("bidId");
        std::optional<int64_t> bidId;
        if (bidIdQuery.has_value()) {
            int64_t valueQuery_instance;
            if (fromStringValue(bidIdQuery.value(), valueQuery_instance)) {
                bidId = valueQuery_instance;
            }
        }
        auto amountPerViewQuery = request.query().get("amountPerView");
        std::optional<double> amountPerView;
        if (amountPerViewQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(amountPerViewQuery.value(), valueQuery_instance)) {
                amountPerView = valueQuery_instance;
            }
        }
        auto amountPerActionQuery = request.query().get("amountPerAction");
        std::optional<double> amountPerAction;
        if (amountPerActionQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(amountPerActionQuery.value(), valueQuery_instance)) {
                amountPerAction = valueQuery_instance;
            }
        }
        auto budgetAmountQuery = request.query().get("budgetAmount");
        std::optional<double> budgetAmount;
        if (budgetAmountQuery.has_value()) {
            double valueQuery_instance;
            if (fromStringValue(budgetAmountQuery.value(), valueQuery_instance)) {
                budgetAmount = valueQuery_instance;
            }
        }
        auto budgetScheduleQuery = request.query().get("budgetSchedule");
        std::optional<std::string> budgetSchedule;
        if (budgetScheduleQuery.has_value()) {
            std::string valueQuery_instance;
            if (fromStringValue(budgetScheduleQuery.value(), valueQuery_instance)) {
                budgetSchedule = valueQuery_instance;
            }
        }
    


        try {





            this->update_bid(version, bidId, deviceId, accountId, amountPerView, amountPerAction, budgetAmount, budgetSchedule, response);
            } catch (Pistache::Http::HttpError &e) {
                response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
                return;
            } catch (std::exception &e) {
                this->handleOperationException(e, response);
                return;
            }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }


}


void BidApi::bid_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api

